import * as types from '../common/ActionTypes'

const initialState = {
}

export default function common(state = initialState, action) {
    switch (action.type) {
        case ...

        default:
            return state
    }
}