import * as types from '../common/ActionTypes'

const initialState = {
}

export default function input(state = initialState, action) {
    switch (action.type) {
        case ...

        default:
            return state
    }
}
